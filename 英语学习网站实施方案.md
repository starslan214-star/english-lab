# English Lab 英语学习网站实施方案

> 面向零基础成年人的英语学习网站，覆盖日常英语、软件测试英语和计算机术语。  
> 文档日期：2026-09-20

## 1. 先说结论

这个网站可以部署到公网，不需要用户和服务器处于同一个局域网。

但不建议把完整系统只放在 GitHub Pages 上。GitHub Pages 是静态网站托管服务，只适合发布 HTML、CSS、JavaScript 和图片等前端文件，不支持在服务器上运行 PHP、Python 等后端程序。AI 模型调用、翻译密钥保护、账号同步、书籍处理和数据库都需要后端服务。

推荐采用以下组合：

| 部分 | 推荐服务 | 用途 |
|---|---|---|
| 前端 | GitHub Pages 或 Cloudflare Pages | 页面、动画、单词游戏、阅读器、PWA |
| 后端 API | Cloudflare Workers / Pages Functions | AI、翻译、词典聚合、新闻源代理 |
| 数据库 | Cloudflare D1 | 用户、词汇掌握度、阅读记录、日志 |
| 文件存储 | Cloudflare R2 | 用户上传的 EPUB、TXT、PDF 和导出文件 |
| 本地离线数据 | IndexedDB | 单机学习记录、离线词库、断网可用 |
| 代码与自动部署 | GitHub | 版本管理、GitHub Actions、公开或私有仓库 |

第一版可以不做注册：学习数据保存在浏览器 IndexedDB 中，并允许导出/导入 JSON 与 Markdown。第二版再增加账号和跨设备同步，开发风险和成本会低很多。

## 2. 当前项目已经具备的能力

当前仓库不是空项目，已经有一套可继续迭代的 MVP：

- 词汇页：5,765 个去重词条，包含四级、六级和场景筛选。
- 单词学习：搜索、分页、朗读、已学/复习状态。
- 词典页：站内 ECDICT 派生数据、Free Dictionary API 英英解释。
- 翻译：用户可以连接自己的百度翻译凭据。
- AI 助教：用户可以连接自己的 DeepSeek API Key，进行解释、分析、故事和测验。
- 阅读：内置 A1 分级练习，并读取部分官方技术博客的近期文章信息。
- 学习笔记：保存查询结果和个人注释，导出 Markdown。
- 安全基础：DeepSeek 与百度密钥由服务端加密后写入 HttpOnly Cookie，前端脚本不能读取。
- PWA 基础：已有 Web App Manifest 和 Service Worker，可继续完善离线使用。

因此接下来的工作重点不是“重写网站”，而是补齐游戏化学习、完整阅读器、图书导入、PDF 导出、学习计划和跨设备同步。

## 3. 产品结构

### 3.1 首页与学习路径

首页只给零基础用户三个明确入口：

1. 今天背 10 个单词。
2. 阅读一篇 3 分钟短文。
3. 和 AI 助教练习 5 分钟。

首页显示：今日完成度、连续学习天数、待复习数量、最近阅读、今日建议。默认用简单英文展示主要内容，重要操作和难点提供中文辅助；随着等级提升逐步减少中文。

### 3.2 背单词板块

词库分类：

- 考试：中考、高考、四级、六级、考研、雅思、托福。
- 日常场景：问候、购物、餐厅、交通、旅行、就医、办公。
- 软件测试：测试类型、缺陷、测试用例、接口测试、自动化测试、性能测试。
- 计算机：操作系统、网络、数据库、编程、云计算、AI。
- 自定义：用户粘贴、上传 CSV，或从阅读内容中收藏。

每个词条建议包含：

```text
单词、音标、真人/合成发音、词性、中文释义、简单英英释义、
词根词缀、联想串记、常见搭配、A1/A2 例句、例句翻译、来源、难度标签
```

学习模式：

- 翻卡记忆：先看英文和图片，再显示解释。
- 听音选词：播放发音，从四个选项中选择。
- 拼写游戏：看图片或中文输入单词。
- 单词配对：英文与释义、词根或场景配对。
- 情境闯关：在餐厅、机场、Bug 评审等动画场景中选词。
- AI 小故事：从本轮生词中选择 5—10 个，生成 A1 小故事和测验。

掌握度不要只用“掌握/未掌握”两个状态。建议使用间隔重复算法，保存：

```text
正确次数、错误次数、连续正确、上次学习时间、下次复习时间、
当前熟练度、反应时长、最近使用场景
```

MVP 可以使用简化 SM-2；以后再升级为 FSRS。用户仍可手动标记“认识”“模糊”“不会”。

### 3.3 英语阅读板块

支持三类内容：

1. 站内短文：团队整理的 A1—B2 分级文章。
2. 公版或开放授权图书：显示来源、作者、授权和原始页面。
3. 用户自有文件：上传 TXT、Markdown、EPUB；PDF 先提取文本，扫描版需要 OCR。

阅读器核心交互：

- 单击单词：显示音标、词性、简单英英释义、中文辅助、加入生词本。
- 选中短语：显示短语意义、搭配和例句。
- 选中句子：显示句子结构、主谓宾、时态、从句和简化表达。
- 精读模式：逐句解释、隐藏翻译、跟读、听写和段落问答。
- 记录：阅读进度、阅读时长、收藏句子、生词和个人笔记。
- 导出：导出“原文 + 笔记 + 生词 + 精读分析”的学习版 PDF。

书源建议：

- Project Gutenberg：只引入明确标注为公版或允许使用的条目，并保留作品落地页链接。其公版判断主要依据美国法律；在中国或其他地区发布前仍需单独确认当地版权状态。
- Google Books API：用于搜索元数据、封面和允许展示的预览，不应把受版权保护的整本内容复制到本站。
- 官方博客、开放教育资源和 Creative Commons 内容：逐条保存许可证与来源。
- 外刊推荐：保存标题、摘要、难度、来源链接和学习导读；没有转载许可时不要在站内复制全文。

### 3.4 词典与翻译板块

界面应明确区分“词典释义”“机器翻译”“AI 解释”，避免用户把 AI 生成内容误认为牛津等权威词典原文。

建议数据层级：

1. 本地开放词典：快速、离线、可全文搜索。
2. 免费英英 API：补充发音和英英定义。
3. 用户自行开通的百度、有道等正式 API。
4. 跳转到 Google 翻译、百度翻译、牛津词典的官方页面。
5. AI 解释：生成更简单的英文释义、场景例句和记忆方法，并标注“AI 生成”。

注意：不能直接抓取或复制牛津、有道、网易等受版权和服务条款保护的词典内容。若没有正式授权或 API，只能提供官方链接，不能在产品中声称“已载入”。

### 3.5 AI 助教板块

建议先做统一的模型适配层，再逐个接入模型：

```text
前端 -> /api/ai/chat -> 模型适配器 -> DeepSeek/OpenAI/其他模型
```

这样前端不需要知道每家模型的请求格式。模型配置包含：提供商、模型名、API 地址、密钥、温度、最大输出长度和是否支持流式输出。

适合零基础成人的预设能力：

- 用 A1 英文解释英文，必要时补一句中文。
- 翻译后解释“为什么这样翻”。
- 分析句子结构、时态、固定搭配。
- 用所选单词生成日常、软件测试或计算机场景短文。
- 生成填空、选择、拼写、听力和对话练习。
- 对用户写的英语进行纠错，并提供“最小修改版”和“自然表达版”。
- 根据当天错误自动安排复习。

生产环境不要把平台统一 API Key 写进前端代码或 GitHub 仓库。可采用两种模式：

- BYOK：用户输入自己的 Key，由后端加密保存；当前项目已采用这种方式。
- 平台代付：Key 只放在后端 Secret 中，同时必须增加登录、额度、限流和费用控制。

### 3.6 每日学习日志

每天自动汇总：

```markdown
# 2026-09-20 英语学习日志

## 今日概览
- 学习时长：25 分钟
- 新学单词：10
- 复习单词：18
- 阅读：1 篇 / 620 词

## 已掌握词汇
- verify — to check that something is correct

## 仍需复习
- regression — 回归；回归测试

## 阅读笔记
- 原句：...
- 我的理解：...

## AI 助教建议
- 明天先复习 5 个软件测试词汇，再读一篇 A1 短文。
```

纯网页无法在用户关闭页面后自动写入其电脑任意文件夹。第一版应提供“一键下载 `.md`”；如果需要每天定时生成并跨设备保存，则由后端定时任务生成，保存到对象存储，或在用户授权后同步到网盘/笔记服务。

## 4. 推荐技术架构

```text
浏览器（手机/电脑）
  ├─ React/Vue/原生 JavaScript 页面
  ├─ IndexedDB：离线学习数据
  ├─ Service Worker：离线缓存
  └─ PDF/EPUB 阅读与导出
          │ HTTPS
          ▼
Cloudflare Worker / Pages Functions
  ├─ 认证与限流
  ├─ AI 模型代理
  ├─ 翻译与词典聚合
  ├─ 图书解析任务
  └─ 每日日志任务
          │
          ├─ D1：结构化数据
          └─ R2：上传文件与导出文件
```

如果只供自己使用，可先保留现在的原生 JavaScript 架构。只有在页面数量和团队规模明显增长后，再迁移到 React + TypeScript；不应为了“技术更新”立即重写已有功能。

## 5. 数据结构建议

```sql
users(id, email, created_at)
vocabularies(id, word, phonetic, level, categories, source, license)
word_senses(id, vocabulary_id, part_of_speech, definition_en, definition_zh)
examples(id, vocabulary_id, sentence_en, sentence_zh, level, source)
user_words(user_id, vocabulary_id, state, ease, interval_days, due_at,
           correct_count, wrong_count, last_seen_at)
books(id, title, author, language, level, source_url, license, storage_key)
reading_sessions(id, user_id, book_id, location, seconds, created_at)
annotations(id, user_id, book_id, selection, note, kind, created_at)
study_logs(id, user_id, study_date, markdown, created_at)
```

所有外部内容至少保存 `source`、`source_url`、`license` 和 `retrieved_at`，方便版权审计和更新。

## 6. 分阶段开发计划

### 阶段 0：整理现有 MVP（1 周）

- 给现有功能补充自动化测试和错误日志。
- 将浏览器 localStorage 数据升级为 IndexedDB，并保留迁移逻辑。
- 完善 PWA 安装、离线提示、数据导出和数据恢复。
- 为所有词典、词库和文章补充来源及许可证说明。
- 增加隐私说明：哪些内容留在本地，哪些内容会发送给 AI 或翻译服务。

验收：手机和电脑均可访问；断网后仍可背已缓存的单词；换浏览器前可备份数据。

### 阶段 1：可持续背词（2—3 周）

- 建立统一词条结构。
- 加入简化 SM-2 复习队列。
- 完成听音选词、拼写、配对三个小游戏。
- 增加日常、软件测试、计算机三个核心场景词库。
- AI 可从本轮词汇生成 A1 小故事和 5 道题。

验收：用户每天进入首页即可完成“新词 + 复习 + 小测”，刷新页面不会丢失进度。

### 阶段 2：阅读器（3—5 周）

- 支持 TXT、Markdown、EPUB 导入；PDF 先做可复制文本的版本。
- 实现点词、选短语、选句分析和收藏。
- 保存阅读位置与笔记。
- 支持学习版 PDF 导出。
- 建立公开内容来源与许可证白名单。

验收：一本 EPUB 可导入、阅读、查词、继续上次位置，并导出带笔记 PDF。

### 阶段 3：账号与同步（2—4 周）

- 邮箱验证码或 OAuth 登录。
- D1 保存学习数据，R2 保存用户文件。
- IndexedDB 与云端增量同步，处理多端冲突。
- 增加数据删除和账号注销功能。

验收：用户在手机学习后，可在电脑继续；离线记录联网后可同步。

### 阶段 4：高级能力（持续迭代）

- FSRS 复习算法。
- 跟读录音、语音识别和发音反馈。
- 每日外刊推荐与难度自动分级。
- 多模型路由、用量统计、预算上限。
- 后台内容管理、词条审核和错误反馈。

## 7. GitHub Pages 部署步骤

GitHub Pages 方案适用于“静态前端 + 独立后端 API”。当前 Worker 服务端代码不能直接在 GitHub Pages 中运行。

### 7.1 建立 GitHub 仓库

```bash
git init
git add .
git commit -m "初始化 English Lab"
git branch -M main
git remote add origin https://github.com/starslan214-star/english-lab.git
git push -u origin main
```

不要提交 `.env`、API Key、Cookie 加密密钥或用户上传文件。GitHub Pages 网站本身是公开可访问的，即使某些付费方案允许源仓库设为私有，也不能把密钥放进静态文件。

### 7.2 准备静态构建

当前项目的静态文件位于 `dist/`。需要确保：

- `dist/index.html` 是入口。
- 所有资源使用相对路径，或正确设置仓库子路径 `/english-lab/`。
- 前端的 API 基地址指向实际 Worker 域名，例如 `https://api.example.com`。
- 单页应用刷新时需要 404 回退方案；当前多模块单页可优先使用 hash 路由。

### 7.3 使用 GitHub Actions 发布 `dist/`

创建 `.github/workflows/pages.yml`：

```yaml
name: 部署 GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - name: 发布
        id: deployment
        uses: actions/deploy-pages@v4
```

然后在 GitHub 仓库中打开：

```text
Settings -> Pages -> Build and deployment -> Source -> GitHub Actions
```

推送到 `main` 后，等待 Actions 完成，即可通过下面的地址访问：

```text
https://你的用户名.github.io/english-lab/
```

### 7.4 部署后端

推荐把当前 `worker/` 改造成 Cloudflare Worker 或 Pages Functions：

1. 创建 Cloudflare 账号并进入 Workers & Pages。
2. 连接 GitHub 仓库，或使用 Wrangler 部署。
3. 设置服务端 Secret：`ENGLISH_LAB_COOKIE_KEY`。
4. 配置允许的前端 Origin，只允许正式站点和本地开发地址。
5. 给 `/api/ai/*`、`/api/translate/*` 增加频率限制和请求大小限制。
6. 把 Worker 地址写进前端环境配置。
7. 如果前后端跨域，正确配置 CORS；更推荐使用同一自定义域名下的 `/api` 反向代理。

生成 32 字节 Cookie 密钥的示例：

```bash
openssl rand -hex 32
```

只把结果设置为云端 Secret，不要写入 Git、JavaScript 或 Markdown 文档。

### 7.5 自定义域名与 HTTPS

- 在 GitHub Pages 设置中添加自己的域名。
- 按 GitHub 给出的记录配置 DNS。
- 打开 `Enforce HTTPS`。
- 确保页面中的 API、图片和音频全部使用 HTTPS，避免混合内容被浏览器阻止。

## 8. 更简单的推荐部署方案

如果不强制要求“网页必须由 GitHub Pages 提供”，更推荐：

```text
GitHub：保存代码
Cloudflare Pages：部署前端
Cloudflare Pages Functions/Workers：部署 API
D1/R2：保存数据和文件
```

这样仍然是 GitHub 自动部署，但前端与 API 可以放在同一平台和同一域名，Cookie、CORS、路由和环境变量更容易管理。Cloudflare Pages Functions 官方支持在边缘运行服务端代码，并可绑定 D1、R2、KV 等资源。

## 9. 安全、隐私与版权清单

- API Key 永远不进入静态前端、GitHub 仓库、日志和导出文件。
- 密钥使用云端 Secret；用户 BYOK 凭据加密保存并允许一键删除。
- AI 和翻译请求前明确提示“所选文字将发送给第三方服务”。
- 上传文件默认私有；使用随机对象键和短时签名 URL。
- 限制文件类型、单文件大小、每日 AI 次数和接口频率。
- 对用户上传书籍声明：用户应拥有合法副本及处理权限。
- 外刊默认只展示标题、少量摘要、学习提示和原文链接。
- 不抓取付费墙全文，不冒充已获得牛津、有道等商业词典授权。
- AI 生成的释义、例句和精读内容必须明显标注。
- 提供“导出我的数据”“删除我的数据”和隐私说明。

## 10. 测试与验收

每次发布前至少检查：

- Chrome、Edge、Safari 手机端和桌面端布局。
- 键盘可操作、字体大小、颜色对比度和屏幕阅读器标签。
- 单词发音失败、API 超时、无网络、Key 失效、额度不足等错误状态。
- 断网学习、重新联网、清除缓存、升级 Service Worker 后的数据完整性。
- 词库筛选、复习时间计算、日志日期和时区。
- EPUB 大文件、恶意文件名、HTML 注入和超长 AI 输入。
- PDF 导出的分页、中文字体、链接、音标和代码块。
- 不同设备同步冲突和账号删除。

## 11. 预算建议

个人 MVP 可以把固定成本控制在接近 0：静态托管、少量 Worker 请求和本地 IndexedDB 通常足够；AI 和翻译费用由用户自己的 Key 承担。进入多人使用阶段后，成本主要来自 AI Token、文件存储、数据库读写、语音服务和邮件验证，应在后台展示每日用量和预算告警。

不要在方案中写死平台价格。云服务免费额度和模型价格会调整，上线前应查看各平台当日官方定价。

## 12. 建议立即执行的顺序

1. 保留现有 MVP，不重写。
2. 先把现有站点部署到公网测试。
3. 完成 IndexedDB 数据层和完整备份/恢复。
4. 加入间隔复习和三个小游戏。
5. 开发 EPUB/TXT 阅读器、点词与笔记。
6. 做学习版 PDF 与每日 Markdown 日志。
7. 确认确实需要跨设备后，再做登录、D1 和 R2。
8. 最后扩展更多词库、外刊源和模型。

## 13. 官方参考资料

- [GitHub Pages 是静态网站托管服务](https://docs.github.com/en/pages/getting-started-with-github-pages/what-is-github-pages)
- [创建 GitHub Pages 网站及服务端语言限制](https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site)
- [使用 GitHub Actions 发布 Pages](https://docs.github.com/en/pages/getting-started-with-github-pages/configuring-a-publishing-source-for-your-github-pages-site)
- [GitHub Pages HTTPS 配置](https://docs.github.com/en/pages/getting-started-with-github-pages/securing-your-github-pages-site-with-https)
- [GitHub Pages 使用限制](https://docs.github.com/en/enterprise-cloud@latest/pages/getting-started-with-github-pages/github-pages-limits)
- [Cloudflare Pages Functions](https://developers.cloudflare.com/pages/functions/)
- [Cloudflare 资源绑定：D1、R2、KV 等](https://developers.cloudflare.com/pages/functions/bindings/)
- [Google Books API 入门](https://developers.google.com/books/docs/v1/getting_started)
- [Project Gutenberg 权限与许可说明](https://www.gutenberg.org/policy/permission)

